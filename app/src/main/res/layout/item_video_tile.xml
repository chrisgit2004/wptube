<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="2dp">

    <ImageView
        android:id="@+id/tileThumbnail"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/card"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="H,1:1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <View
        android:id="@+id/tileScrim"
        android:layout_width="0dp"
        android:layout_height="46dp"
        android:background="#CC000000"
        app:layout_constraintBottom_toBottomOf="@id/tileThumbnail"
        app:layout_constraintStart_toStartOf="@id/tileThumbnail"
        app:layout_constraintEnd_toEndOf="@id/tileThumbnail" />

    <TextView
        android:id="@+id/tileTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="6dp"
        android:textColor="@color/text_primary"
        android:textSize="12sp"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintBottom_toBottomOf="@id/tileThumbnail"
        app:layout_constraintStart_toStartOf="@id/tileThumbnail"
        app:layout_constraintEnd_toEndOf="@id/tileThumbnail"
        tools:text="video title goes here" />

    <TextView
        android:id="@+id/tileDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:background="#BF000000"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:text="12:04"
        android:textColor="@color/text_primary"
        android:textSize="9sp"
        app:layout_constraintTop_toTopOf="@id/tileThumbnail"
        app:layout_constraintEnd_toEndOf="@id/tileThumbnail" />

</androidx.constraintlayout.widget.ConstraintLayout>
